"use strict";

var currentMonth = 0,
    passedMonth = 0,
    month = new Date().getMonth() + 1;
search.searchDaysFromWeek(0, month, function (n) {
  var t = n.week,
      e = search.getAccumulationWeek(t);
  currentMonth += parseInt(e.accumulation), n.final ? cronos.divCurrentMonth.innerText = currentMonth + " => " + parseInt(currentMonth / 60) + "h " + currentMonth % 60 + "min" : cronos.divCurrentMonth.innerText = currentMonth + " (processando)";
}, null);